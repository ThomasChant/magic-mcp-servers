# Data Source Configuration
# Set to 'true' to use Supabase database, 'false' to use JSON files
VITE_USE_SUPABASE=false

# Supabase Configuration (only needed if USE_SUPABASE=true)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Service key for migration script (keep secret, don't commit)
SUPABASE_SERVICE_KEY=your-service-key-here

# Database Configuration
# You can find these values in your Supabase project settings under API
# URL: Go to Settings > API > Project URL
# Anon Key: Go to Settings > API > Project API keys > anon public key
# Service Key: Go to Settings > API > Project API keys > service_role secret key